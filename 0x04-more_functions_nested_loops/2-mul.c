#include "main.h"
/**
 * mul - checks for multiplication values
 * @a: a - first variable
 * @b: a - second variable
 * Return: Always  0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
